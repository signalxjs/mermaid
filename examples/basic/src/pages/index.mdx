---
title: Diagrams
description: Mermaid fences rendered by @sigx/mermaid
---

# Diagrams

Every block below is a plain ` ```mermaid ` fence in the MDX source. The build
leaves the source in the HTML; `@sigx/mermaid/client` swaps in an SVG once the
diagram scrolls into view.

## Flowchart

```mermaid title="Checkout"
graph LR
  Cart --> Address
  Address --> Payment
  Payment -->|authorised| Confirm
  Payment -->|declined| Address
  Confirm --> Done([Order placed])
```

## Sequence

```mermaid
sequenceDiagram
  participant Browser
  participant CDN
  participant Origin
  Browser->>CDN: GET /orders/42
  CDN-->>Browser: 304 Not Modified
  Browser->>Origin: POST /orders/42/pay
  Origin-->>Browser: 202 Accepted
```

## State

```mermaid title="Order lifecycle"
stateDiagram-v2
  [*] --> Draft
  Draft --> Placed: submit
  Placed --> Shipped: dispatch
  Placed --> Cancelled: cancel
  Shipped --> [*]
  Cancelled --> [*]
```

## Pie

```mermaid
pie title Traffic by source
  "Organic" : 52
  "Referral" : 27
  "Direct" : 21
```

## Other fences are left alone

`rehypeMermaid` claims `language-mermaid` and nothing else — this block renders
however the site normally renders code.

```ts
import { Mermaid } from '@sigx/mermaid';

export function Architecture() {
    return <Mermaid code="graph LR; Client-->API-->DB;" title="Services" />;
}
```

[Second page](/second/) — used by the e2e test to check SPA navigation.
